// Code generated by togo (test) - extend with your own cases.
package rest

import (
	"context"
	"os"
	"testing"

	"github.com/danielgtaylor/huma/v2/humatest"
	"github.com/togo-framework/togo"

	"github.com/togo-framework/demo/internal/app"
)

// TestPostAPI exercises the posts endpoints. Set
// TEST_DATABASE_URL (a migrated DB) to run it; skipped otherwise.
func TestPostAPI(t *testing.T) {
	if os.Getenv("TEST_DATABASE_URL") == "" {
		t.Skip("set TEST_DATABASE_URL to run Post API tests")
	}
	os.Setenv("DATABASE_URL", os.Getenv("TEST_DATABASE_URL"))

	k := togo.New()
	a := app.New(context.Background(), k)
	defer k.Close()
	if a.SQLDB == nil {
		t.Skip("database unavailable")
	}

	_, api := humatest.New(t)
	RegisterPostRoutes(api, a)

	if resp := api.Get("/api/posts"); resp.Code != 200 {
		t.Fatalf("list posts: expected 200, got %d", resp.Code)
	}
}
